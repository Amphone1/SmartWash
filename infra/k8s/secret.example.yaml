# TEMPLATE — copy to secret.yaml, fill real values, apply out-of-band.
# NEVER commit the populated Secret. In prod, source these from a vault
# (e.g. External Secrets Operator / Sealed Secrets), not a plain manifest.
apiVersion: v1
kind: Secret
metadata:
  name: smartwash-secrets
  namespace: smartwash
type: Opaque
stringData:
  DATABASE_URL: "postgresql://smartwash:CHANGE_ME@postgres:5432/smartwash"
  POSTGRES_PASSWORD: "CHANGE_ME"
  INTERNAL_SERVICE_TOKEN: "CHANGE_ME_LONG_RANDOM"
  KEYCLOAK_CLIENT_SECRET: "CHANGE_ME"
  MINIO_ACCESS_KEY: "CHANGE_ME"
  MINIO_SECRET_KEY: "CHANGE_ME"
  GOOGLE_MAPS_API_KEY: "CHANGE_ME"
  OWNER_ACCOUNT: "OWNER-ACC-0001"

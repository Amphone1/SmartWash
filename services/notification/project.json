{
  "name": "notification",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/notification/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p services/notification/tsconfig.app.json",
          "node tools/build-service.mjs notification"
        ],
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/services/notification"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node --project services/notification/tsconfig.app.json -r tsconfig-paths/register services/notification/src/main.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint services/notification/src --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "jest --config services/notification/jest.config.ts" }
    }
  }
}

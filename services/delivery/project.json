{
  "name": "delivery",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/delivery/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p services/delivery/tsconfig.app.json",
          "node tools/build-service.mjs delivery"
        ],
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/services/delivery"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node --project services/delivery/tsconfig.app.json -r tsconfig-paths/register services/delivery/src/main.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint services/delivery/src --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "jest --config services/delivery/jest.config.ts" }
    }
  }
}

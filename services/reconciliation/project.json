{
  "name": "reconciliation",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/reconciliation/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p services/reconciliation/tsconfig.app.json",
          "node tools/build-service.mjs reconciliation"
        ],
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/services/reconciliation"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node --project services/reconciliation/tsconfig.app.json -r tsconfig-paths/register services/reconciliation/src/main.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint services/reconciliation/src --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "jest --config services/reconciliation/jest.config.ts" }
    }
  }
}

{
  "name": "audit",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/audit/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p services/audit/tsconfig.app.json",
          "node tools/build-service.mjs audit"
        ],
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/services/audit"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node --project services/audit/tsconfig.app.json -r tsconfig-paths/register services/audit/src/main.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint services/audit/src --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "jest --config services/audit/jest.config.ts" }
    }
  }
}

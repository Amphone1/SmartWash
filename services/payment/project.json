{
  "name": "payment",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/payment/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p services/payment/tsconfig.app.json",
          "node tools/build-service.mjs payment"
        ],
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/services/payment"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node --project services/payment/tsconfig.app.json -r tsconfig-paths/register services/payment/src/main.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint services/payment/src --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "jest --config services/payment/jest.config.ts" }
    }
  }
}

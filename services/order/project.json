{
  "name": "order",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/order/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p services/order/tsconfig.app.json",
          "node tools/build-service.mjs order"
        ],
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/services/order"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node --project services/order/tsconfig.app.json -r tsconfig-paths/register services/order/src/main.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint services/order/src --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "jest --config services/order/jest.config.ts" }
    }
  }
}

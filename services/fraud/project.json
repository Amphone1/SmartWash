{
  "name": "fraud",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/fraud/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p services/fraud/tsconfig.app.json",
          "node tools/build-service.mjs fraud"
        ],
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/services/fraud"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node --project services/fraud/tsconfig.app.json -r tsconfig-paths/register services/fraud/src/main.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint services/fraud/src --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "jest --config services/fraud/jest.config.ts" }
    }
  }
}

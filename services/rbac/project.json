{
  "name": "rbac",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/rbac/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p services/rbac/tsconfig.app.json",
          "node tools/build-service.mjs rbac"
        ],
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/services/rbac"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node --project services/rbac/tsconfig.app.json -r tsconfig-paths/register services/rbac/src/main.ts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint services/rbac/src --max-warnings 0" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "jest --config services/rbac/jest.config.ts" }
    }
  }
}

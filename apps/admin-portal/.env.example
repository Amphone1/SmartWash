VITE_API_BASE_URL=http://localhost:8088/api
# "keycloak", not localhost — token issuer must match what the auth service
# validates. Add `127.0.0.1 keycloak` to the hosts file (see docs/RUNBOOK.md).
VITE_KEYCLOAK_URL=http://keycloak:8080

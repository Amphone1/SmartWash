{
  "permissions": {
    "allow": [
      "Bash(LAN_IP=172.19.0.107 docker compose -f infra/docker/docker-compose.dev.yml -f infra/docker/docker-compose.lan.yml up -d keycloak auth)",
      "PowerShell(npx nx test bff 2>&1)",
      "PowerShell(npx nx lint payment 2>&1)",
      "Bash(LAN_IP=172.19.0.107 docker compose -f infra/docker/docker-compose.dev.yml -f infra/docker/docker-compose.lan.yml up -d --build payment bff)",
      "Bash(xargs grep -l \"class IdempotencyService\")",
      "Bash(git check-ignore *)",
      "Bash(echo \"probe: $\\(cd /d/SmartWash/infra/docker && docker compose -f docker-compose.dev.yml --dry-run up keycloak 2>&1)",
      "Bash(LAN_IP=172.19.0.107 timeout 150 docker compose -f infra/docker/docker-compose.dev.yml logs -f --tail 0 auth bff)",
      "Bash(docker port *)"
    ]
  }
}
